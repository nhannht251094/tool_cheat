# Tool Cheat React

A Vite + React + TypeScript starter project for a local tool/config dashboard.

## Install

```bash
npm install
```

## Run local

```bash
npm run dev
```

Open the local URL printed in the terminal.

## Build

```bash
npm run build
```

Upload the `dist` folder to any static hosting.
